{
  "name": "@aetherAssembly/core",
  "version": "1.0.0",
  "type": "module",
  "license": "AGPL-3.0-only",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "idb": "^8.0.3"
  }
}
