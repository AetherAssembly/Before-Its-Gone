{
  "name": "before-its-gone-electron",
  "private": true,
  "version": "0.8.0",
  "description": "Electron desktop shell for Before It's Gone",
  "author": {
    "name": "Before-Its-Gone",
    "email": "maintainers@beforeitsgone.local"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AetherAssembly/Before-Its-Gone"
  },
  "main": "dist/main.js",
  "type": "commonjs",
  "scripts": {
    "dev": "npm run build && cross-env ELECTRON_OZONE_PLATFORM_HINT=auto electron .",
    "build": "tsc -p tsconfig.json",
    "package:appimage": "electron-builder --config electron-builder.linux.yml --linux AppImage --publish never -c.electronVersion=42.0.1",
    "package:linux": "electron-builder --config electron-builder.linux.yml --linux --publish never -c.electronVersion=42.0.1",
    "package:linux:arm64": "electron-builder --config electron-builder.linux.yml --linux --arm64 --publish never -c.electronVersion=42.0.1",
    "package:macos": "node -e \"require('fs').mkdirSync('../../release', {recursive: true})\" && electron-builder --config electron-builder.macos.yml --mac dmg --publish never -c.electronVersion=42.0.1",
    "package:windows": "node -e \"require('fs').mkdirSync('../../release', {recursive: true})\" && electron-builder --config electron-builder.windows.yml --win nsis portable --publish never -c.electronVersion=42.0.1",
    "publish:linux": "electron-builder --config electron-builder.linux.yml --linux --publish always -c.electronVersion=42.0.1",
    "publish:linux:arm64": "electron-builder --config electron-builder.linux.yml --linux --arm64 --publish always -c.electronVersion=42.0.1",
    "publish:macos": "node -e \"require('fs').mkdirSync('../../release', {recursive: true})\" && electron-builder --config electron-builder.macos.yml --mac dmg --publish always -c.electronVersion=42.0.1",
    "publish:windows": "node -e \"require('fs').mkdirSync('../../release', {recursive: true})\" && electron-builder --config electron-builder.windows.yml --win nsis portable --publish always -c.electronVersion=42.0.1"
  },
  "dependencies": {
    "electron-updater": "^6.3.9",
    "nodemailer": "^8.0.7",
    "qrcode": "^1.5.4",
    "resend": "^6.12.3",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "@types/qrcode": "^1.5.5"
  }
}
